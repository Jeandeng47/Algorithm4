
public interface _Search {
    // Pick a vertex and mark all vertices that are connected to it
    public boolean marked(int v);
    // Count the number of vertices that are connected to the vertex
    public int count();
}
